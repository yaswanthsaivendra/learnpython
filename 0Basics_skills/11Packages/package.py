# Packages are namespaces which contains multiple packages and modules within themselves.
#Each package in python is a directory which must contain a special file called __init__.py . This file can be empty.
#It indicates the directory it contains as python package. It can be imported the same way a module can be imported.

import package.module
from package.module import fun_name


